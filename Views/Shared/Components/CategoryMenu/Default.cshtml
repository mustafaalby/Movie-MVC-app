@model IEnumerable<Category>
<div class="list-group">
    <a href="/" class="list-group-item list-group-item-action action">Tüm Kategoriler</a>
    @foreach (var item in Model)
    {
        <a asp-controller="Home" asp-action="index" asp-route-id="@item.id"
         class="list-group-item list-group-item-action @(ViewBag.SelectedCategory==item.id.ToString()?"active":"")">@item.Name</a>
    }
    
</div>